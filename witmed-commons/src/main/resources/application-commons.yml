### 配置开启 Spring Boot 项目 Bean id 的 Override 机制
spring:
  main:
    allow-bean-definition-overriding: true
### 配置 Eureka Server 连接地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9999/eureka/